**🧠 Multilingual Text-to-SQL Bot**

A powerful Multilingual Text-to-SQL application that allows users to ask questions in natural language (English / Urdu / Hindi / Roman Urdu) and automatically converts them into SQL queries.
This project uses a LoRA fine-tuned TinyLlama 1.1B model to generate SQL and a Gradio interface for easy interaction.

**🚀 Features**

Converts natural language → SQL queries
Multilingual input support
Lightweight LoRA fine-tuning for small custom datasets
Fully local setup (no paid APIs)
Interactive Gradio web interface

**🛠️ Tech Stack**

Python
PyTorch
HuggingFace Transformers
LoRA / PEFT
Gradio
Scikit-learn

**📁 Project Structure**
Multilingual_Text-to-SQL_Bot/
│
├── app.py                 # Main application file (backend + Gradio UI)
├── requirements.txt       # Python dependencies
├── assets/                # Screenshots / demo images
│   └── gradio_ui.png
├── model/                 # Fine-tuned model folder (auto-created)
│   └── tinyllama_sql_finetuned/
├── dataset/               # Optional dataset (DB / CSV)
│   └── data.db
└── README.md              # Project documentation


**📌 Note: The tinyllama_sql_finetuned folder is automatically created when you run app.py and train the model.**

⚙️ How to Run Locally

1️⃣ Clone the repository
git clone https://github.com/AbdulMohaiman976/Multilingual_Text-to-SQL_Bot.git
cd Multilingual_Text-to-SQL_Bot

2️⃣ Create a virtual environment (Recommended)
python -m venv venv
source venv/bin/activate     # Linux / Mac
venv\Scripts\activate        # Windows

3️⃣ Install requirements
pip install -r requirements.txt

4️⃣ Run the app
python app.py

5️⃣ Open in browser
http://127.0.0.1:7860  # Gradio default

☁️ How to Run on Google Colab

1️⃣ Open Google Colab

2️⃣ Upload the repository files (app.py, requirements.txt, dataset/)
3️⃣ Install dependencies:

!pip install -r requirements.txt


4️⃣ Run the app:

!python app.py

🧪 Example Queries

"Lahore ke customers dikhao"

"Show me all orders from 2024"

"Highest scoring student ka record lao"

➡️ Output: Valid SQL queries automatically generated by the model.

**💡 Why This Project is Useful**

Makes SQL accessible for non-technical users
Eliminates the need to write SQL manually
Lightweight fine-tuned model suitable for small datasets
Can be extended for any relational database
